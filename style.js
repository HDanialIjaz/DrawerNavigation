import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container:{
        backgroundColor:"#fff",
        alignItems:"center",
        justifyContent:'center',
        flex:1
    },
    text:{
        fontSize:20,
        fontWeight:"bold"
    }
});